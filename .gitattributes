# Normalise to LF in the repo so the Linux Docker build and Windows dev see the
# same bytes. Without this, autocrlf turns every source file CRLF on checkout.
* text=auto eol=lf
*.png binary
*.pdf binary
*.docx binary
